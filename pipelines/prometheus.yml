format_version: 10
pipelines:
  prometheus:
    group: Tools
    materials:
      git-da613e5:
        git: https://github.com/chaitus09/tools-automation-code.git
        shallow_clone: false
        auto_update: true
        branch: main
    stages:
      - Ansible:
          fetch_materials: true
          approval:
            type: manual
          jobs:
            ansible:
              tasks:

                - exec: {command: ansible-playbook, arguments: [ "-i", "54.88.152.146,", "tools.yml", "-e", "tool_name=prometheus", "-e", "ansible_user=ec2-user", "-e", "ansible_password=DevOps321" ], run_if: passed}                global:



global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "static-targets"
    static_configs:
      - targets:
          - "localhost:9090"
          - "172.31.23.163:9100"




